#ifndef ROMAN_H
#define ROMAN_H

// Function to convert an integer to a Roman numeral
char* toRoman(int num);

// Function to manually calculate string length (no need for string.h)
int stringLength(const char* str);

// Function to manually concatenate two strings (no need for string.h)
void stringConcat(char* destination, const char* source);

#endif
